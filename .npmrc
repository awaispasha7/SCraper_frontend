# pnpm-specific configuration for frontend
# Note: npm will show warnings about unknown configs - this is expected and harmless
# These settings only apply when using pnpm, not npm
auto-install-peers=true
strict-peer-dependencies=false

